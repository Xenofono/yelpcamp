<%include ../partials/header %>

    <div class="row">
        <div class="centerform">
            <h1 style="text-align: center;">Redigera campingen <%=campground.name%></h1>
            <form action="/campgrounds/<%=campground._id%>?_method=PUT" method="POST">
                <div class="form-group">
                    <input class="form-control" type="text" name="campground[name]" placeholder="name" value="<%=campground.name%>">
                </div>
                <div class="form-group">
                    <input class="form-control" type="number" name="campground[price]" value="<%=campground.price%>" min="0" step="1">
                </div>
                <div class="form-group">
                    <input class="form-control" type="text" name="campground[image]" placeholder="image url" value="<%=campground.image%>">
                </div>
                <div class="form-group">
                    <textarea class="form-control" name="campground[description]" rows="5" placeholder="description"><%=campground.description%></textarea>
                </div>
                <div class="form-group">
                    <button class="btn btn-lg btn-secondary btn-block btn-primary">Lägg till</button>
                </div>
                <a href="/campgrounds">Tillbaka till campingar</a>
        </div>
        </form>
    </div>

</div>
<%include ../partials/footer %>